# Commands (legacy)

Slash commands are deprecated in favor of [skills](../skills/). This directory is reserved for future command assets and is copied to `.cursor/commands/` during bootstrap.

Prefer adding workflows under `skills/` with `SKILL.md` frontmatter.
